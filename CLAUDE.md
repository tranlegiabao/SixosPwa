# Project Instructions

Treat `.agents/AGENTS.md` as the main operating guide. Read repository context in `.agents/context/` and choose the relevant skill under `.agents/skills/` before substantial work.

Use verified code and documentation as sources of truth. Make the smallest correct change, preserve existing conventions, run relevant checks, and distinguish verified facts from assumptions. Do not expose secrets or perform destructive actions without explicit authorization.
